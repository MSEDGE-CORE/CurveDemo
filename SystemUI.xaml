<Page
    x:Class="CurveDemo.SystemUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurveDemo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True"
    SizeChanged="Page_SizeChanged">

    <Grid>
        <Frame x:Name="DesktopFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            
        </Frame>
        <Frame x:Name="StatusBarFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        </Frame>
        <Button x:Name="GstBut" Click="GstBut_Click" Opacity="0" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Button.KeyboardAccelerators>
                <KeyboardAccelerator Key="Escape"/>
            </Button.KeyboardAccelerators>
        </Button>
    </Grid>
</Page>
